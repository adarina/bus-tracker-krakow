import { GetVehicleResponse } from './get-vehicle-response';

describe('GetVehicleResponse', () => {
  it('should create an instance', () => {
    expect(new GetVehicleResponse()).toBeTruthy();
  });
});
